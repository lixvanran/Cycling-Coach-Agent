[project]
name = "cycling-coach-backend"
version = "0.1.0"
description = "公路自行车 AI 教练 · 后端 Sidecar"
requires-python = ">=3.11"
readme = "../README.md"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
